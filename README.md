# ACPET scavenger hunt

Next.js app. Clues live in DynamoDB table `clue-app` (`us-west-2`).

```
npm install
npm run dev
```

- Hunt: `/k8v3q` and the other trail codes
- Admin: `/admin` (password in `.env.local`)
